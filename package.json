{
  "name": "context-viewer",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "cd viewer && bunx vite",
    "build": "cd viewer && bunx vite build",
    "preview": "cd viewer && bunx vite preview",
    "test": "cd viewer && bunx vitest run",
    "test:watch": "cd viewer && bunx vitest",
    "test:analyzer": "cd analyzer && vitest run",
    "test:all": "cd analyzer && vitest run && cd ../viewer && vitest run"
  },
  "devDependencies": {
    "vite": "^5.4.8",
    "vitest": "^2.1.3"
  }
}
